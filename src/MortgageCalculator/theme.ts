import { ThemeInterface } from './types';

export const themes: ThemeInterface = {
	light: {
		background: 'white',
		textColor: 'black'
	},

	dark: {
		background: 'black',
		textColor: 'white'
	}
};
